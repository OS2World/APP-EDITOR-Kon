/*  FILE *file = fopen( "test", "w" );

  for( int f = 0; f < fontList->fonts; f++ )
  {
    fprintf( file, "%d: %s\n", f, fontList->font[f].name );
    for( int s = 0; s < fontList->font[f].sizes; s++ )
    {
      fprintf( file, "    %d, %d, %d\n", fontList->font[f].size[s].pointSize,
               fontList->font[f].size[s].X, fontList->font[f].size[s].Y );
    }
  }

  fclose( file );*/

